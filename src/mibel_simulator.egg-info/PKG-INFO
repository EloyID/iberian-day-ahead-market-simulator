Metadata-Version: 2.4
Name: mibel-simulator
Version: 0.1.0
Summary: MIBEL Iberian day-ahead electricity market clearing simulator
Author: EloyID
License-Expression: MIT
Project-URL: Homepage, https://github.com/EloyID/mibel-simulator
Project-URL: Repository, https://github.com/EloyID/mibel-simulator
Project-URL: Bug Tracker, https://github.com/EloyID/mibel-simulator/issues
Keywords: electricity market,MIBEL,OMIE,day-ahead market,market clearing,energy,Iberian peninsula
Classifier: Development Status :: 3 - Alpha
Classifier: Intended Audience :: Science/Research
Classifier: Topic :: Scientific/Engineering
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Operating System :: OS Independent
Requires-Python: >=3.10
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: numpy
Requires-Dist: pandas
Requires-Dist: pandera
Requires-Dist: pyomo
Provides-Extra: solver
Requires-Dist: gurobipy; extra == "solver"
Provides-Extra: dev
Requires-Dist: pytest>=7.0; extra == "dev"
Requires-Dist: pytest-cov; extra == "dev"
Requires-Dist: wandb; extra == "dev"
Dynamic: license-file

# mibel-simulator

A Python simulator for the **MIBEL** (Mercado Ibérico de Electricidad) Iberian day-ahead electricity market clearing process.

The package replicates the OMIE/MIBEL market-clearing algorithm, including:
- Simple and complex bid orders
- Paradoxical order detection and iterative resolution
- Spain–Portugal interconnection capacity constraints
- France fixed-exchange integration
- Residual demand curve computation and interpolation

## Requirements

### Solver support

The simulator uses Pyomo `SolverFactory`, so you can choose the solver with `solver_factory_type`.

- Recommended/default: **Gurobi** (`solver_factory_type="gurobi"`)
- Also possible: **CBC**, **GLPK**, or any solver plugin available in your Pyomo environment

> Note: model performance and feasibility behavior can vary by solver. Gurobi is the most tested option in this project.

If you use Gurobi, install `gurobipy` and configure your licence:
```bash
pip install gurobipy
```

If you use CBC/GLPK, install the corresponding solver binaries in your system and use the appropriate `solver_factory_type`.

## Installation

```bash
pip install mibel-simulator
```

Or install from source:
```bash
git clone https://github.com/EloyID/mibel-simulator.git
cd mibel-simulator
pip install -e .
```

## Quick Start

```python
import pandas as pd
from mibel_simulator import (
    parse_cab_file,
    parse_det_file,
    parse_capacidad_inter_file,
    run_mibel_simulator,
)

# Parse raw OMIE input files
cab = parse_cab_file("path/to/cab_file.txt")
det = parse_det_file("path/to/det_file.txt")
capacidad_inter = parse_capacidad_inter_file("path/to/capacidad_inter.txt")

# Run the clearing simulation
france_day_ahead_prices = pd.DataFrame(
    {
        "int_period": [1, 2, 3],
        "float_price_fr": [55.0, 60.0, 58.0],
    }
)

results = run_mibel_simulator(
    cab=cab,
    det=det,
    capacidad_inter_pbc=capacidad_inter,
    france_day_ahead_prices=france_day_ahead_prices,
    solver_factory_type="gurobi",  # or "cbc", "glpk", ...
    solver_options={"MIPGap": 0.001, "TimeLimit": 60},
)
print(results["clearing_prices"])
```

## Project Structure

```
src/mibel_simulator/
├── clearing_process.py        # Main iterative clearing loop
├── make_model.py              # Pyomo MILP model builder
├── run_model.py               # Pyomo solver wrapper (configurable solver)
├── data_preprocessor.py       # Input data processing
├── parse_omie_files.py        # OMIE flat-file parsers
├── paradoxal_orders_tools.py  # Paradoxical order utilities
├── residual_demand_curve.py   # RDC computation & interpolation
├── schemas/                   # Pandera data-validation schemas
└── data/                      # Bundled reference data
```

## Development

```bash
pip install -e ".[dev]"
pytest
```

## Licence

[MIT](LICENSE)
